<UserControl x:Class="VisualMaster.Communication.UI.TcpDriverConfigControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             Background="{DynamicResource ColorRootBg}"
             Foreground="White"
             FontSize="15">
    <UserControl.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <ResourceDictionary Source="../Themes/CommunicationDarkTheme.xaml"/>
            </ResourceDictionary.MergedDictionaries>
            <Style TargetType="ComboBox" BasedOn="{StaticResource {x:Type ComboBox}}">
                <Setter Property="Height" Value="34"/>
                <Setter Property="FontSize" Value="15"/>
                <Setter Property="VerticalContentAlignment" Value="Center"/>
                <Setter Property="VerticalAlignment" Value="Center"/>
            </Style>
        </ResourceDictionary>
    </UserControl.Resources>
    <Grid Margin="12">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="128"/>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="128"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="48"/>
            <RowDefinition Height="48"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        <TextBlock Grid.ColumnSpan="4" Text="TCP 网络驱动" Foreground="White" FontWeight="SemiBold" Margin="0,0,0,12"/>

        <TextBlock Grid.Row="1" Text="IP 地址"/>
        <TextBox x:Name="IpBox" Grid.Row="1" Grid.Column="1"
                 Text="{Binding IpAddress, UpdateSourceTrigger=PropertyChanged}"/>

        <TextBlock Grid.Row="2" Text="端口"/>
        <TextBox x:Name="PortBox" Grid.Row="2" Grid.Column="1"
                 Text="{Binding Port, UpdateSourceTrigger=PropertyChanged}"/>

        <Border Grid.Row="3" Grid.ColumnSpan="4" Background="#242424" BorderBrush="#444444" BorderThickness="1" Padding="10" Margin="0,14,0,8">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="128"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>
                <TextBlock Text="绑定块名称"/>
                <TextBox x:Name="BlockNameBox" Grid.Column="1"
                         Text="{Binding BlockName, UpdateSourceTrigger=PropertyChanged}"/>
                <Button Grid.Column="2" Content="实时" MinWidth="78" Height="34" FontSize="15" Margin="12,0,0,0" Click="OnRealtimeClick"/>
            </Grid>
        </Border>
    </Grid>
</UserControl>
